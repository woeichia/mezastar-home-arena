# PWA deployment

GitHub Pages builds the production app from `main` through `.github/workflows/deploy-pages.yml`.

Public URL: https://woeichia.github.io/mezastar-home-arena/

Collection data is local to each browser/device. Export a JSON backup before clearing browser storage, uninstalling the PWA or changing phones.
